let a: int = 34 + 35;
let b: int = 10 - 05;
let c: int = 06 * 10;
let d: int = 100 / 4;

// will be replaced when strs eventually are implemented
let future_str: str = "%d\n";
extrn printf "printf"
printf(0, &future_str, a);
printf(0, &future_str, b);
printf(0, &future_str, c);
printf(0, &future_str, d);
